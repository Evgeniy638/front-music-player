import Head from 'next/head';

const MusicPlayerHead = () :JSX.Element => {
    return (
        <Head>
            <title>Music player</title>
            <meta name="viewport" content="initial-scale=1.0, width=device-width" />
        </Head>
    );
}

export default MusicPlayerHead;
